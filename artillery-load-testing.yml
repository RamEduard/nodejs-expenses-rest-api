config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - flow:
    - get:
        url: "/categories"
    - get:
        url: "/records/expense"
    - get:
        url: "/records/income"